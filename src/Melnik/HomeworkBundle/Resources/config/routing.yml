melnik_homework_homepage:
    pattern:  /{_locale}/index
    defaults: { _controller: MelnikHomeworkBundle:Default:index, _locale: en }
    requirements:
            _locale: en|uk

melnik_homework_about:
    pattern: /about
    defaults: { _controller: MelnikHomeworkBundle:Default:about }

melnik_homework_contact:
    pattern: /contact
    defaults: { _controller: MelnikHomeworkBundle:Default:contact }

melnik_homework_cars:
    pattern: /car
    defaults: { _controller: MelnikHomeworkBundle:Default:car}
